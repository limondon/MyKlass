{
  "name": "enot-pomogun",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "hosting:serve": "npm run build && firebase emulators:start --only hosting",
    "deploy:hosting": "npm run build && firebase deploy --only hosting",
    "deploy:rules": "firebase deploy --only database",
    "deploy": "npm run build && firebase deploy --only hosting,database",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "firebase": "^12.14.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "4.3.4",
    "firebase-tools": "^15.19.1",
    "vite": "6.4.3",
    "vitest": "4.1.8"
  }
}
